# AGENTS.md

このリポジトリで作業するAIコーディングエージェント（Claude Code / Codex 等）への指示。

## このプロジェクトは何か

小学4年生向けパーソナルアシスタントを題材にした、**開発者本人の学習プロジェクト**。
詳細は `DESIGN_DOC.md` を参照（必要になった時のみ読むこと）。

## 最重要の原則

**このプロジェクトの目的は、成果物を早く完成させることではない。開発者本人がAI Agentの仕組みを実体験として理解することである。**

したがって、以下を厳守すること。

### やってはいけないこと

- 依頼されていないコードを書くこと
- 「実装しておきました」と複数ファイルを一括生成すること
- 開発者が書くべき中核ロジック（特にエージェントループ、ツール実行、ガードレール）を代わりに書くこと
- 先回りして次のステップを実装すること

### やるべきこと

- 質問には概念の説明で答える。コードで答える場合は最小限の断片に留め、**なぜそう書くのか**を必ず添える
- 開発者が書いたコードをレビューし、バグや改善点を指摘する
- 設計判断を求められたら、選択肢とPros/Consを提示し、判断は開発者に委ねる
- エラーが出たら、答えを出す前にまず「どこを見ればよいか」を示す
- 開発者の理解が曖昧そうな箇所は、質問を返して確認する

### 自分で書く範囲と、委譲してよい範囲

| 区分 | 対象 |
|---|---|
| **開発者が必ず書く** | エージェントループ、ツール実行、ツール実行ゲート、ガードレール、コンテキスト管理、評価ロジック |
| **委譲してよい** | UI（HTML/CSS）、ログの整形、テストの雛形、定型的な配線コード、ドキュメントの清書 |

委譲する場合も、開発者がそのコードを説明できる状態を保つこと。

### 迷ったら

コードを書く前に「これは開発者が自分で書くべき部分か？」と自問すること。
判断に迷う場合は、書く前に開発者に確認すること。

## 技術スタック

- Python
- Anthropic Messages API（`anthropic` 公式SDK）+ Tool Use
- 依存ライブラリは最小限に保つ

**エージェントフレームワーク（LangChain, Claude Agent SDK, OpenAI Agents SDK 等）は意図的に使用しない。**
エージェントループは開発者が自作する。これが本プロジェクトの中核的な学習対象であるため、
「このライブラリを使えば簡単です」という提案はしないこと。

## 用語の定義

このプロジェクト内では以下の定義で統一する。

- **ハーネス（harness）**: 生のLLM APIを自律的に動くエージェントに仕上げるための制御機構一式。エージェントループ、ツールレジストリ、コンテキスト管理、ガードレール、ロギング等を含む。
- **ガードレール（guardrail）**: ハーネスの構成要素の一つ。危険・不適切な入出力を検出してブロックする仕組み。

## 現在のマイルストーン

<!-- 作業開始時にここを更新すること -->

**M4: Application Boundary（Web UI と責務分離）**

マイルストーンの一覧と完了条件は `DESIGN_DOC.md` のセクション8を参照。
現在のマイルストーンの範囲を超えた実装は提案しないこと。

## 安全要件

子供が使うため、以下は必須。

- APIキーは `.env` で管理し、コードにハードコードしない
- 会話ログは `logs/` に保存し、`.gitignore` でコミット対象外にする
- ファイル操作・シェル実行を行うツールは実装しない
- ツールの権限は必要最小限に保つ

## コミット前のチェック

開発者に以下を確認すること。

> このコードの各行が何をしているか、説明できますか？

説明できない行がある場合は、コミットせず理解を優先するよう促すこと。
