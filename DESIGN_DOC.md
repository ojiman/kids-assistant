# Design Doc: 子供向けパーソナルアシスタント（学習プロジェクト）

- **ステータス**: Draft（レビュー待ち）
- **最終更新**: 2026-09-13
- **オーナー**: Norio

---

## 1. 目的

### 1.1 第一目的（最優先）

**開発者自身が手を動かし、AI Agentの仕組みを実体験として理解すること。**

具体的には、以下を「自分の言葉で説明できる」状態を目指す。

- LLM APIの生の呼び出しと、AI Agentの違いは何か
- ハーネス（agent harness）とは何を指すのか、何を構成要素とするのか
- ガードレールはハーネスのどの部分にあたり、どう実装するのか
- Tool Use（Function Calling）のリクエスト/レスポンスループは実際にどう回るのか
- コンテキスト管理（履歴保持・圧縮）はなぜ必要で、どう実装するのか

### 1.2 第二目的

小学4年生の子供が、学習や日常の疑問を安心して相談できるアシスタントを実際に使える状態にすること。

**重要**: 1.2は1.1を達成した結果として得られる副産物と位置づける。1.2を急ぐために1.1を犠牲にしてはならない。

---

## 2. 非ゴール（Non-Goals）

以下は本プロジェクトのスコープ外とする。

- 商用品質・プロダクションレベルの可用性
- 家族以外への提供、公開サービス化
- 最新・最高性能のモデルを使うこと（学習目的にはコスト効率の良いモデルで十分）
- 既存のエージェントフレームワーク／ハーネスを使いこなすこと（後述の通り、むしろ意図的に避ける）
- 完成を急ぐこと

---

## 3. 学習モードの原則（開発プロセス上の最重要制約）

本プロジェクトでは、AIコーディングエージェント（Claude Code / Codex）を **教師・レビュアー** として使い、**代筆者** としては使わない。

### 3.1 エージェントに依頼してよいこと

- 概念の説明、設計判断の選択肢提示とPros/Cons整理
- 自分が書いたコードのレビュー、バグの指摘、改善提案
- エラーメッセージの解読、デバッグの伴走
- 公式ドキュメントの該当箇所の案内
- テストコードの雛形作成（本体ロジックではないため）
- 質問に答える形での、部分的なコード例の提示（**写経して理解する前提**）

### 3.2 エージェントに依頼してはいけないこと

- 「この機能を実装して」と丸投げし、生成物をそのまま採用すること
- 複数ファイルにまたがる自動生成・自動リファクタリング
- 理解していないコードをコミットすること

### 3.3 自己チェック

コミット前に必ず自問する。

> **このコードの各行が何をしているか、他人に説明できるか？**

説明できない行が1行でもあれば、コミットせずに理解するまで質問する。

---

## 4. ユーザーとユースケース

### 4.1 想定ユーザー

- **主ユーザー**: 小学4年生（9-10歳）。英語環境の学校に通っており、中学受験準備中。
- **副ユーザー（監督者）**: 親（開発者本人）。会話ログを確認できる必要がある。

### 4.2 想定ユースケース

| # | ユースケース | 必要な機能 |
|---|---|---|
| U1 | 「〜ってどういう意味？」という言葉・概念の質問 | LLM応答のみ |
| U2 | 算数の問題の解き方を聞く | LLM応答 + 計算ツール |
| U3 | 「なんで空は青いの？」のような素朴な疑問 | LLM応答のみ |
| U4 | 英単語の意味・使い方を聞く | LLM応答（辞書ツールは任意） |
| U5 | 親が「今日何を聞いたか」を確認する | ログ機能 |

### 4.3 明示的に対応しないユースケース

- 宿題の答えをそのまま出力すること（考え方を教える方針とする）
- 自由なWeb検索（安全性の担保が困難なため、初期スコープ外）
- 画像の生成・解析

---

## 5. 技術選定

### 5.1 選定の前提となる整理：抽象度のレイヤー

AI Agent開発の選択肢は、抽象度によって以下のレイヤーに分かれる。

| レイヤー | 内容 | 代表例 |
|---|---|---|
| L1: 生のAPI | 1回のリクエスト/レスポンス。ループは自作 | Anthropic Messages API, OpenAI Responses API |
| L2: ハーネス/SDK | エージェントループ、ツール実行、コンテキスト管理を提供 | Claude Agent SDK, OpenAI Agents SDK, DeepSeek Harness |
| L3: マネージド実行環境 | 上記に加えセッション管理・サンドボックス・復旧をホスト側が担う | OpenAI Agents API |

### 5.2 用語の定義（本プロジェクト内での統一）

- **ハーネス（harness）**: 生のLLM APIを「自律的に動くエージェント」に仕上げるための、エージェント専用の制御機構一式。ツールレジストリ、エージェントループ、コンテキスト管理、メモリ、ガードレール、可観測性などを含む。
- **ガードレール（guardrail）**: 上記ハーネスの構成要素の一つ。危険な操作・不適切な内容を検出し、ブロックまたは確認を求める仕組み。

### 5.3 決定：L1（生のAPI）を採用する

**採用**: Anthropic Messages API + Tool Use（Python）

**理由**:

- 第一目的（仕組みの理解）に対し、L2/L3はエージェントループを隠蔽してしまい、学習効果が大きく損なわれる
- L1で自作することで、L2/L3が何を肩代わりしているのかを後から理解できるようになる
- 子供向けという要件上、ツールは最小限でよく、L2/L3の強力な機能（ファイル操作、シェル実行、サンドボックス）はオーバースペックかつ安全上のリスク
- 普段Claude Codeを使っているため、Anthropicのエコシステムで一貫させた方が、質問→検証のループがスムーズ

**採用しなかった選択肢**:

| 選択肢 | 不採用の理由 |
|---|---|
| OpenAI Responses API | L1として同等に妥当。ただしエコシステムの一貫性でAnthropicを優先。**将来的に比較検証する価値はあるため、Phase 2の検討事項として残す** |
| Claude Agent SDK | L2。コーディングエージェント向けの強力な機能が前提で、本用途には過剰かつ学習効果が低い |
| OpenAI Agents SDK | L2。同上。ガードレール機構は参考になるが、まず自作すべき |
| OpenAI Agents API | L3。長時間・複雑なタスク向け。本用途には完全にオーバースペックで、サンドボックス課金も不要 |
| DeepSeek Harness | L2。OSSでセルフホスト可能な点は興味深いが、developer preview段階で破壊的変更が明記されており、また子供の会話データの取り扱いという観点で追加検討が必要 |
| LangChain / LangGraph | 抽象化層が厚く、学習目的には不向き |

### 5.4 言語

**採用**: Python

- エージェント関連のドキュメント・サンプルが最も豊富で、学習時の情報アクセスが良い
- Anthropic公式SDKの第一級サポート言語

**補足**: 開発者はServer-side Kotlin（Spring Boot）に習熟している。Phase 3以降で、理解した内容をKotlinで再実装することは、理解度の検証として有効な選択肢となる（後述）。

### 5.5 依存ライブラリの方針

**最小限に留める**。以下のみを許容する。

- `anthropic`（公式SDK）
- `python-dotenv`（APIキー管理）
- Phase 3以降でUIが必要になった時点で、Webフレームワーク（FastAPI等）を追加

エージェントフレームワーク系のライブラリは、本プロジェクトでは **意図的に使用しない**。

---

## 6. アーキテクチャ

### 6.1 全体構成（Phase 3完了時点の想定）

```
[ユーザー（子供）]
        │
        ▼
[インターフェース層]  ← CLI or シンプルなWeb UI（Phase 3で決定）
        │
        ▼
┌───────────────────────────────────────┐
│  自作ハーネス層                          │
│                                        │
│  ┌──────────────────────────────┐     │
│  │ 入力ガードレール                │     │
│  │ - 不適切な話題の検出            │     │
│  └──────────────────────────────┘     │
│              │                         │
│              ▼                         │
│  ┌──────────────────────────────┐     │
│  │ エージェントループ              │     │
│  │ 1. 履歴 + 入力を API に送信     │     │
│  │ 2. tool_use があればツール実行   │     │
│  │ 3. 結果を履歴に追加して 1へ戻る  │     │
│  │ 4. tool_use がなければ終了       │     │
│  └──────────────────────────────┘     │
│         │            │                 │
│         ▼            ▼                 │
│  ┌───────────┐  ┌──────────────┐     │
│  │ツールレジストリ│  │コンテキスト管理│     │
│  │- 計算       │  │- 履歴保持     │     │
│  │- （拡張可）  │  │- 圧縮/打ち切り │     │
│  └───────────┘  └──────────────┘     │
│              │                         │
│              ▼                         │
│  ┌──────────────────────────────┐     │
│  │ 出力ガードレール                │     │
│  │ - 不適切な応答の検出            │     │
│  └──────────────────────────────┘     │
│              │                         │
│              ▼                         │
│  ┌──────────────────────────────┐     │
│  │ ロギング（親の確認用）           │     │
│  └──────────────────────────────┘     │
└───────────────────────────────────────┘
        │
        ▼
[Anthropic Messages API]
```

### 6.2 ディレクトリ構成（案）

```
kids-assistant/
├── AGENTS.md              # AIコーディングエージェント向け指示（Claude Code / Codex 共通）
├── CLAUDE.md              # AGENTS.md へのポインタのみ
├── DESIGN_DOC.md          # 本ドキュメント
├── README.md
├── .env.example
├── requirements.txt
├── src/
│   ├── main.py            # エントリポイント
│   ├── harness/
│   │   ├── loop.py        # エージェントループ
│   │   ├── context.py     # コンテキスト管理
│   │   └── logger.py      # ロギング
│   ├── tools/
│   │   ├── registry.py    # ツール定義とディスパッチ
│   │   └── calculator.py  # 計算ツール
│   └── guardrails/
│       ├── input.py       # 入力ガードレール
│       └── output.py      # 出力ガードレール
├── logs/                  # 会話ログ（.gitignore対象）
└── tests/
```

**注意**: この構成は最初から全部作らない。マイルストーンに沿って段階的に育てる（M0では `main.py` 1ファイルのみ）。

---

## 7. マイルストーン

各マイルストーンは「動くものができる」単位で区切る。各マイルストーン完了時に、**何を学んだかを自分の言葉で書き残す**（`docs/learnings/` 等）。

### M0: 素のAPI呼び出し（ツールなし）

**ゴール**: Anthropic Messages APIを叩いて応答を得る。1往復のみ。

**学習ポイント**:
- リクエスト/レスポンスの構造（`messages`, `system`, `content` の形）
- システムプロンプトが応答をどう変えるか

**完了条件**: CLIで質問を入力し、応答が返る。

---

### M1: 会話履歴の保持

**ゴール**: 複数ターンの会話が成立する。

**学習ポイント**:
- LLM APIはステートレスであり、履歴は毎回送る必要があるという事実を体感する
- トークン数が会話とともに増えていくことを実測する

**完了条件**: 「さっきの話だけど」が通じる。

---

### M2: Tool Use の実装（★最重要）

**ゴール**: 計算ツールを1つ定義し、エージェントループを自作する。

**学習ポイント**:
- ツール定義（JSON Schema）の書き方
- `stop_reason: tool_use` の検出
- ツール実行 → `tool_result` を履歴に追加 → 再度APIを呼ぶ、というループ
- **このループこそがハーネスの中核であることを体感する**

**完了条件**: 「123かける456は？」に対し、モデルが計算ツールを呼び、正しい答えを返す。

**注意**: このマイルストーンが本プロジェクトで最も学習価値が高い。ここだけは絶対に生成物をコピペせず、自分で書くこと。

---

### M3: ガードレールの実装

**ゴール**: 子供向けの安全機構を入れる。

**実装項目**:
- 入力ガードレール: 明らかに不適切な話題のキーワード検出
- システムプロンプトによる振る舞い制御（年齢に合った語彙、宿題の答えを直接教えない等）
- 出力ガードレール: 応答内容のチェック
- ロギング: 全会話をタイムスタンプ付きで保存

**学習ポイント**:
- システムプロンプトによる制御だけでは不十分な理由（プロンプトインジェクションの存在）
- 決定論的なコードによるチェックと、LLMによるチェックの使い分け
- ガードレールがハーネスの一部品であることの実感

**完了条件**: 意図的に不適切な入力をした際にブロックされ、ログに記録される。

---

### M4: インターフェース整備

**ゴール**: 子供が実際に使える形にする。

**未決事項のため、M3完了時点で改めて設計する**（後述の Open Questions 参照）。

---

### Phase 2以降（オプション）

理解度の検証と深化のための選択肢。優先度順ではなく、興味に応じて選ぶ。

- **OpenAI Responses API で M2 相当を再実装**: 同じレイヤーの別実装を比較し、共通点と差分から本質を抽出する
- **Claude Agent SDK を使ってみる**: 自作した後に使うことで、「何を肩代わりしてくれているか」が明確に分かる
- **Kotlin / Spring Boot で再実装**: 得意言語で書き直すことで理解度を検証する
- **コンテキスト圧縮の実装**: 会話が長くなった際の要約処理

---

## 8. 安全設計（詳細）

子供が使うという前提から、以下を必須要件とする。

### 8.1 必須要件

| ID | 要件 | 実装マイルストーン |
|---|---|---|
| S1 | 全会話をログに記録し、親が後から確認できる | M3 |
| S2 | 不適切な話題への応答を拒否する | M3 |
| S3 | ツールの権限は必要最小限とする（ファイル操作・シェル実行は実装しない） | M2 |
| S4 | APIキーをコードにハードコードしない（.env管理、.gitignore） | M0 |
| S5 | 会話ログをリポジトリにコミットしない | M3 |

### 8.2 設計原則

- **最小権限**: ツールは「安全に完結するもの」のみ。外部への書き込み、コマンド実行は一切実装しない。
- **多層防御**: システムプロンプトのみに依存せず、コード側のチェックも併用する。
- **可観測性**: 何が起きたか後から追えることを、機能追加より優先する。

---

## 9. Open Questions → 決定済み事項

### Q1: インターフェース形態 ✅ 決定

**決定**: ローカルWeb UI（M4で実装）

- M0–M3はCLIで進める（変更なし）
- M4は FastAPI + 素のHTML/JS。React/Vue等のフロントフレームワークは使わない
- 第一目的（学習）を妨げない実装規模に留める

### Q2: 実行環境・ホスティング ✅ 決定

**決定**: ローカルPCのみ

- クラウド・家庭内サーバー（Raspberry Pi等）は不要
- 子供の会話ログを外部に出さないという安全方針とも一致

### Q3: 子供のアクセス端末 ✅ 決定

**決定**: 家族共有デバイス（自分専用のスマホ/タブレットなし）

- これによりLINE Bot（外部公開必要）は不要と確定
- ローカルWeb UIへの接続は家庭内LAN内で完結する想定

### Q4: 使用モデル

学習目的にはコスト効率の良いモデルで十分と思われるが、子供向けの応答品質とのバランスをM0で実測して決める。

---

## 10. 開発ツールの扱い（Claude Code / Codex 両対応）

### 10.1 方針

仕様と指示を **ツール非依存の形で外部化** し、どちらのツールからでも同じ文脈で開発できるようにする。

### 10.2 実現方法

- `AGENTS.md` を **唯一の情報源（Single Source of Truth）** とする
  - Codexが読む標準ファイル
  - Claude Codeも同ファイルを参照可能
- `CLAUDE.md` は `AGENTS.md` を参照するだけの薄いポインタとする（内容を二重管理しない）
- 本 `DESIGN_DOC.md` をリポジトリに含め、`AGENTS.md` から参照させる

### 10.3 トークン消費の最適化

- `AGENTS.md` は簡潔に保つ（長大な指示は毎回のコンテキストを圧迫する）
- 詳細は `DESIGN_DOC.md` に置き、必要時のみ読ませる
- 1セッション = 1マイルストーン を目安にセッションを区切る
- 「実装して」ではなく「レビューして」「なぜこうなるか説明して」という使い方が、結果的にトークン効率も良い

---

## 11. 完了の定義

本プロジェクトは、以下を満たした時点で第一目的を達成したとみなす。

- [ ] エージェントループを自分で実装し、その動作を説明できる
- [ ] ハーネスとガードレールの違いを、自分の実装コードを指しながら説明できる
- [ ] 既存のハーネス（Claude Agent SDK等）が何を肩代わりしているか説明できる
- [ ] 子供が実際に使い、親が会話ログを確認できる状態になっている

---

## 12. レビュー依頼事項

本ドキュメントについて、以下の観点で確認をお願いします。

1. 第一目的（学習）と第二目的（実用）のバランスは意図通りか
2. マイルストーンの粒度は適切か（大きすぎ／小さすぎないか）
3. Open Questions（特にQ3: 子供の端末）について、現時点で決められる情報があるか
4. スコープに過不足はないか
